export function ReadingPracticeStyles() {
  return null;
}

// Export the styles separately to be included in the parent component
export const readingPracticeStyles = `
  .reading-practice-page {
    height: calc(100vh - 2rem);
    max-height: calc(100vh - 2rem);
    background: linear-gradient(135deg, #0f0f1a 0%, #1a1a2e 50%, #16213e 100%);
    padding: 1rem;
    overflow: hidden;
    box-sizing: border-box;
  }
`;
